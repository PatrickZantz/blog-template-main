---
import BaseHead from '@/components/BaseHead'
import Header from '@/components/Header'
import Footer from '@/components/Footer'
import ProviderTheme from '@/components/ProviderTheme'
import ProviderAnimations from '@/components/ProviderAnimations'
// import TwSizeIndicator from '@/components/TwSizeIndicator'
import EditBlog from '@/components/editBlog'
import background from '../assets/images/image.png'

const { title, description, image, articleDate } = Astro.props
---

<html lang='en' class='scroll-smooth'>
	<head>
		<BaseHead title={title} description={description} ogImage={image} articleDate={articleDate} />
		<ProviderTheme />
		<ProviderAnimations />
	</head>

	<body class='bg-white text-stone-950 dark:bg-[#0a0910] dark:text-white'>
		<img id='background' src={background.src} alt='' fetchpriority='high' />

		<main
			class='px-5 sm:mx-auto sm:max-w-2xl sm:px-8 lg:px-0 antialiased md:max-w-6xl grid gap-12 mt-0 overflow-hidden md:overflow-visible'
		>
			<Header />
			<slot />
			<Footer />
		</main>
		<!-- <TwSizeIndicator /> -->
		<EditBlog />
		<style>

			body {
				margin-left: calc(100vw - 100%); /* prevent layout shift */
			}

			#background {
				margin: 0;
				padding: 0;
				opacity: 0.3;
				z-index: -10;
				position: absolute;
				justify-content: center;
			}
		</style>
	</body>
</html>
